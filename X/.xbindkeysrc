# thumb wheel up => increase volume
"pactl set-sink-volume @DEFAULT_SINK@ +2%"
   b:6

# thumb wheel down => lower volume
"pactl set-sink-volume @DEFAULT_SINK@ -2%"
   b:7

# side button bottom => show window switcher and application launcher in rofi
"rofi -show window"
   b:8

# side button top => show window switcher in rofi
"rofi -combi-modi window,run -show combi"
   b:9
